# Base URL for an OpenAI-compatible API (do not include trailing slash or include /v1/chat/completions)
VITE_AI_API_URL=
# API key for the provider
VITE_AI_API_KEY=
# Optional: override the chat model used for SPC recommendations
VITE_AI_MODEL=gpt-4o-mini
